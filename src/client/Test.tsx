import React, { Component } from "react"

export default class Text extends React.Component {
    render() {
        return (
            <div>This is a JS React component running within Typescript</div>
        )
    }
}